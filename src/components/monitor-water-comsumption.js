// utils/waterConsumption.js

export function monitorWaterConsumption(waterConsumption) {
    const thresholdLimit = 200; // Example threshold limit
    
    // Check if water consumption exceeds the threshold
    return waterConsumption > thresholdLimit;
  }
  